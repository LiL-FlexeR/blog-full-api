import React from "react";

const FirstScreen = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      FirstScreen
    </div>
  );
};

export default FirstScreen;
